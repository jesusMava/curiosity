<p> User: <%= comment.user.email %></p>
<p>
  <strong>Message:</strong>
  <%= comment.message %>
</p>
<%= link_to "Edit", edit_curiosity_card_comment_path(@curiosity, comment.id)%>
<%= link_to "Destroy", curiosity_card_comment_path(@curiosity, comment.id),
  data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %>
<hr class='has-background-grey-light'/>
